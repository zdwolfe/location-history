{
  "description": "Java example function with existing pom.xml",
  "handler": "locator.add.AddLocation::handler",
  "runtime": "java",
  "hooks": {
    "build": "mvn package",
    "clean": "mvn clean"
  }
}